name: Build

on:
  push:
    branches:
      - "**"

concurrency:
  group: orchestrator-build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Orchestrator (${{ matrix.target }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: linux-arm64
            goos: linux
            goarch: arm64
            goarm: ""
          - target: macos-arm64
            goos: darwin
            goarch: arm64
            goarm: ""

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: orchestrator/go.mod
          cache-dependency-path: orchestrator/go.sum

      - name: Ensure builds directory exists
        run: mkdir -p builds

      - name: Build orchestrator
        working-directory: orchestrator
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          GOARM: ${{ matrix.goarm }}
        run: go build -o ../builds/vinyl-orchestrator-${{ matrix.target }} .

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: vinyl-orchestrator-${{ matrix.target }}
          path: builds/vinyl-orchestrator-${{ matrix.target }}

  build-frontend:
    name: Build Flutter Web
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: "stable"

      - name: Install Flutter dependencies
        working-directory: web_app
        run: flutter pub get

      - name: Build Flutter Web
        working-directory: web_app
        run: flutter build web --release

      - name: Upload Web Assets Artifact
        uses: actions/upload-artifact@v4
        with:
          name: flutter-web-assets
          path: web_app/build/web
